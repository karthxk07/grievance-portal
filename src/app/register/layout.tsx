import { Metadata } from "next";
import Page from "./page"; // import your Demo's page

export const metadata: Metadata = {
  title: "Register",
  description: "AIMK's Grievance Portal's Registration",
};

export default Page;
